# EX-006

Illustrative excerpt; not a standalone runnable example.

```text
def run_job(input_path, run_id, logger):
    logger.info("start", extra={"run_id": run_id})
    data = read_and_validate(input_path)
    result = transform(data)
    write_result(result, run_id)
    logger.info("done", extra={"run_id": run_id})
```
