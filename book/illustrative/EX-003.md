# EX-003

Illustrative excerpt; not a standalone runnable example.

```text
map.on('load', () => {
  map.addSource('facilities', {
    type: 'geojson',
    data: '/api/v1/facilities.geojson'
  });

  map.addLayer({
    id: 'facilities-circle',
    type: 'circle',
    source: 'facilities',
    paint: { 'circle-radius': 5 }
  });
});
```
