# EX-004

Illustrative excerpt; not a standalone runnable example.

```text
GET /api/v1/features?bbox=34.40,31.45,34.55,31.60&limit=200

Rules:
- limit <= 500
- bbox area <= configured maximum
- allowed filters only
- geometry returned only when requested
```
