# Offline-first sketch

العميل يعمل من قاعدة محلية. كل تعديل يضاف إلى `sync_queue` بمعرف عملية ثابت. عند توفر الشبكة تُرسل العمليات بالتتابع. الاستجابة `409 version_conflict` لا تعامل كخطأ شبكة؛ تُعرض للمستخدم أو تطبق عليها قاعدة merge محددة.
