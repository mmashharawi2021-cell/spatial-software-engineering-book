# Benchmarks

Performance examples belong here. Record dataset size, hardware/runtime environment, query plan, warm/cold cache state, and repeated measurements. Never present a single local timing as a universal benchmark.
